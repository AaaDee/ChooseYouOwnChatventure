# Build-time variables read by docker-compose and passed to the frontend build
# as build args. Copy this file to `.env` (gitignored) and fill in the values.
#
# Google Tag Manager container ID, e.g. GTM-XXXXXXX. Leave empty to disable GTM.
VITE_GTM_ID=
